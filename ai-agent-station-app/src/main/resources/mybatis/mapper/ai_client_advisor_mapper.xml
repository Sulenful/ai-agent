<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingfeng.infrastructure.dao.IAiClientAdvisorDao">

    <resultMap id="BaseResultMap" type="com.lingfeng.infrastructure.dao.po.AiClientAdvisor">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="advisor_id" property="advisorId" jdbcType="VARCHAR"/>
        <result column="advisor_name" property="advisorName" jdbcType="VARCHAR"/>
        <result column="advisor_type" property="advisorType" jdbcType="VARCHAR"/>
        <result column="order_num" property="orderNum" jdbcType="INTEGER"/>
        <result column="ext_param" property="extParam" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_client_advisor (advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time)
        VALUES (#{advisorId}, #{advisorName}, #{advisorType}, #{orderNum}, #{extParam}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO ai_client_advisor (advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.advisorId}, #{item.advisorName}, #{item.advisorType}, #{item.orderNum}, #{item.extParam}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_client_advisor WHERE id = #{id}
    </delete>

    <delete id="deleteByCondition" parameterType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor">
        DELETE FROM ai_client_advisor
        <where>
            <if test="advisorId != null and advisorId != ''">
                AND advisor_id = #{advisorId}
            </if>
            <if test="advisorName != null and advisorName != ''">
                AND advisor_name = #{advisorName}
            </if>
            <if test="advisorType != null and advisorType != ''">
                AND advisor_type = #{advisorType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </delete>

    <update id="update" parameterType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor">
        UPDATE ai_client_advisor
        <set>
            <if test="advisorId != null and advisorId != ''">
                advisor_id = #{advisorId},
            </if>
            <if test="advisorName != null and advisorName != ''">
                advisor_name = #{advisorName},
            </if>
            <if test="advisorType != null and advisorType != ''">
                advisor_type = #{advisorType},
            </if>
            <if test="orderNum != null">
                order_num = #{orderNum},
            </if>
            <if test="extParam != null">
                ext_param = #{extParam},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_advisor
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" parameterType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_advisor
        <where>
            <if test="advisorId != null and advisorId != ''">
                AND advisor_id = #{advisorId}
            </if>
            <if test="advisorName != null and advisorName != ''">
                AND advisor_name = #{advisorName}
            </if>
            <if test="advisorType != null and advisorType != ''">
                AND advisor_type = #{advisorType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        LIMIT 1
    </select>

    <select id="selectList" parameterType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_advisor
        <where>
            <if test="advisorId != null and advisorId != ''">
                AND advisor_id = #{advisorId}
            </if>
            <if test="advisorName != null and advisorName != ''">
                AND advisor_name LIKE CONCAT('%', #{advisorName}, '%')
            </if>
            <if test="advisorType != null and advisorType != ''">
                AND advisor_type = #{advisorType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY order_num ASC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_advisor
        ORDER BY order_num ASC
    </select>

    <select id="count" parameterType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM ai_client_advisor
        <where>
            <if test="advisorId != null and advisorId != ''">
                AND advisor_id = #{advisorId}
            </if>
            <if test="advisorName != null and advisorName != ''">
                AND advisor_name LIKE CONCAT('%', #{advisorName}, '%')
            </if>
            <if test="advisorType != null and advisorType != ''">
                AND advisor_type = #{advisorType}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="queryByAdvisorId" resultType="com.lingfeng.infrastructure.dao.po.AiClientAdvisor">
        SELECT id, advisor_id, advisor_name, advisor_type, order_num, ext_param, status, create_time, update_time
        FROM ai_client_advisor
        WHERE advisor_id = #{advisorId}
    </select>

</mapper>