<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingfeng.infrastructure.dao.IAiClientSystemPromptDao">

    <resultMap id="BaseResultMap" type="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="prompt_id" property="promptId" jdbcType="VARCHAR"/>
        <result column="prompt_name" property="promptName" jdbcType="VARCHAR"/>
        <result column="prompt_content" property="promptContent" jdbcType="LONGVARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, prompt_id, prompt_name, prompt_content, description, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_client_system_prompt (prompt_id, prompt_name, prompt_content, description, status, create_time, update_time)
        VALUES (#{promptId}, #{promptName}, #{promptContent}, #{description}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO ai_client_system_prompt (prompt_id, prompt_name, prompt_content, description, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.promptId}, #{item.promptName}, #{item.promptContent}, #{item.description}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_client_system_prompt WHERE id = #{id}
    </delete>

    <delete id="deleteByCondition" parameterType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt">
        DELETE FROM ai_client_system_prompt
        <where>
            <if test="promptId != null and promptId != ''">
                AND prompt_id = #{promptId}
            </if>
            <if test="promptName != null and promptName != ''">
                AND prompt_name = #{promptName}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </delete>

    <update id="update" parameterType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt">
        UPDATE ai_client_system_prompt
        <set>
            <if test="promptId != null and promptId != ''">
                prompt_id = #{promptId},
            </if>
            <if test="promptName != null and promptName != ''">
                prompt_name = #{promptName},
            </if>
            <if test="promptContent != null">
                prompt_content = #{promptContent},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_system_prompt
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" parameterType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_system_prompt
        <where>
            <if test="promptId != null and promptId != ''">
                AND prompt_id = #{promptId}
            </if>
            <if test="promptName != null and promptName != ''">
                AND prompt_name = #{promptName}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        LIMIT 1
    </select>

    <select id="selectList" parameterType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_system_prompt
        <where>
            <if test="promptId != null and promptId != ''">
                AND prompt_id = #{promptId}
            </if>
            <if test="promptName != null and promptName != ''">
                AND prompt_name LIKE CONCAT('%', #{promptName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_client_system_prompt
        ORDER BY create_time DESC
    </select>

    <select id="count" parameterType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM ai_client_system_prompt
        <where>
            <if test="promptId != null and promptId != ''">
                AND prompt_id = #{promptId}
            </if>
            <if test="promptName != null and promptName != ''">
                AND prompt_name LIKE CONCAT('%', #{promptName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="queryByPromptId" resultType="com.lingfeng.infrastructure.dao.po.AiClientSystemPrompt">
        SELECT id, prompt_id, prompt_name, prompt_content, description, status, create_time, update_time
        FROM ai_client_system_prompt
        WHERE prompt_id = #{promptId}
    </select>

</mapper>