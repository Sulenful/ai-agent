<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lingfeng.infrastructure.dao.IAiAgentTaskScheduleDao">

    <resultMap id="BaseResultMap" type="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule">
        <id column="id" property="id" jdbcType="BIGINT"/>
        <result column="agent_id" property="agentId" jdbcType="BIGINT"/>
        <result column="task_name" property="taskName" jdbcType="VARCHAR"/>
        <result column="description" property="description" jdbcType="VARCHAR"/>
        <result column="cron_expression" property="cronExpression" jdbcType="VARCHAR"/>
        <result column="task_param" property="taskParam" jdbcType="LONGVARCHAR"/>
        <result column="status" property="status" jdbcType="TINYINT"/>
        <result column="create_time" property="createTime" jdbcType="TIMESTAMP"/>
        <result column="update_time" property="updateTime" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_agent_task_schedule (agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time)
        VALUES (#{agentId}, #{taskName}, #{description}, #{cronExpression}, #{taskParam}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" parameterType="java.util.List">
        INSERT INTO ai_agent_task_schedule (agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.agentId}, #{item.taskName}, #{item.description}, #{item.cronExpression}, #{item.taskParam}, #{item.status}, #{item.createTime}, #{item.updateTime})
        </foreach>
    </insert>

    <delete id="deleteById" parameterType="java.lang.Long">
        DELETE FROM ai_agent_task_schedule WHERE id = #{id}
    </delete>

    <delete id="deleteByCondition" parameterType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule">
        DELETE FROM ai_agent_task_schedule
        <where>
            <if test="agentId != null">
                AND agent_id = #{agentId}
            </if>
            <if test="taskName != null and taskName != ''">
                AND task_name = #{taskName}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </delete>

    <update id="update" parameterType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule">
        UPDATE ai_agent_task_schedule
        <set>
            <if test="agentId != null">
                agent_id = #{agentId},
            </if>
            <if test="taskName != null">
                task_name = #{taskName},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="cronExpression != null">
                cron_expression = #{cronExpression},
            </if>
            <if test="taskParam != null">
                task_param = #{taskParam},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        WHERE id = #{id}
    </update>

    <select id="selectById" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_agent_task_schedule
        WHERE id = #{id}
    </select>

    <select id="selectByCondition" parameterType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_agent_task_schedule
        <where>
            <if test="agentId != null">
                AND agent_id = #{agentId}
            </if>
            <if test="taskName != null and taskName != ''">
                AND task_name = #{taskName}
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        LIMIT 1
    </select>

    <select id="selectList" parameterType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_agent_task_schedule
        <where>
            <if test="agentId != null">
                AND agent_id = #{agentId}
            </if>
            <if test="taskName != null and taskName != ''">
                AND task_name LIKE CONCAT('%', #{taskName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM ai_agent_task_schedule
        ORDER BY create_time DESC
    </select>

    <select id="count" parameterType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule" resultType="java.lang.Integer">
        SELECT COUNT(*)
        FROM ai_agent_task_schedule
        <where>
            <if test="agentId != null">
                AND agent_id = #{agentId}
            </if>
            <if test="taskName != null and taskName != ''">
                AND task_name LIKE CONCAT('%', #{taskName}, '%')
            </if>
            <if test="status != null">
                AND status = #{status}
            </if>
        </where>
    </select>

    <select id="queryAllValidTaskSchedule" resultType="com.lingfeng.infrastructure.dao.po.AiAgentTaskSchedule">
        SELECT id, agent_id, task_name, description, cron_expression, task_param, status, create_time, update_time
        FROM ai_agent_task_schedule
        WHERE status = 1
        ORDER BY create_time DESC
    </select>

    <select id="queryAllInvalidTaskScheduleIds" resultType="java.lang.Long">
        SELECT id
        FROM ai_agent_task_schedule
        WHERE status = 0
        ORDER BY create_time DESC
    </select>

</mapper>